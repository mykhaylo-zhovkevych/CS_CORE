
1. The method can take up to numbers, sparated by comasan will return teri sum.
for example "1" or, "1,2"^as inputs. (for each ampty string, it will return 0)


2. Allow the Add method to handle an unknown amount if numbers 


3. Allow the Add method to handle new lines between numbers (instead of commas).

the following input is ok: "1\n2,3" will be 6
the following input is NOT ok: "1,\n"

4. Support different delimiters
to chnage a delimiter, the beginning of the string will contain a separate line that looks like this:
"//[delimiter]\n[numbers...]"" for example : "//;\n1;n2" should return 3 where the default delimiter is ';''
The first line is optional. All existing scenarios should still be supported.


5. Calling Add with a negative number will throw an exception "negatives not allowed" and the negative that was passed.
If there are mulitple negatives, show all of them in the excewption message.

